#ifndef TB_SERVER
#define TB_SERVER

#include <vector>
#include <string>
#include <thread>

namespace TorrentialBits {
    class Server {
        public:
            Server();
        private:
    };
}
#endif